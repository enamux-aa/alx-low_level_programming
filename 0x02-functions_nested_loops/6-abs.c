#include "main.h"

/**
 * _abs - compute the absolute number ogf n
 *
 * @n: the parameter to be tested
 *
 * Return: 1
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-(n));
	}
	else
	{
		return (n);
	}
}

